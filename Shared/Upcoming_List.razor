@code {

    public struct Upcoming
    {
        public string name;
        public string project;
        public bool in_progress; //If true (in progress), false (in planning)
    };

    static Upcoming cs_basics = new Upcoming()
    {
        name = "C# Basics",
        project = "Books",
        in_progress = true
    };

    static Upcoming gist_rust = new Upcoming()
    {
        name = "Gist of Rust",
        project = "Books",
        in_progress = false
    };

    static Upcoming c_basics = new Upcoming()
    {
        name = "C Basics",
        project = "Books",
        in_progress = false
    };

    static Upcoming swift_basics = new Upcoming()
    {
        name = "Swift Basics",
        project = "Books",
        in_progress = false
    };

    public static readonly Upcoming[] Upcomings = new[] {cs_basics, gist_rust, c_basics, swift_basics};
}